name: jao_example
description: Dart Frog API demonstrating jao ORM with SQLite.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.6.0 <4.0.0'

resolution: workspace

dependencies:
  dart_frog: ^1.2.0
  jao:
    path: ../jao
  jao_cli:
    path: ../jao_cli

dev_dependencies:
  build_runner: ^2.4.0
  jao_generator:
    path: ../jao_generator
  test: ^1.24.0
  lints: ^6.0.0
